# تقرير وخطة الأمان — FlowCash (الجولة السابعة)

مراجعة كاملة زي ما طلبت، مبنية على قراءة الكود الفعلي مش افتراضات. اللي اتصلح اتصلح في الكود مباشرة، واللي محتاج قرار منك أو خطوة يدوية في Vercel/GitHub/Supabase موضّح تحت بالتفصيل.

## اكتشاف مهم لازم تعرفه فورًا

كان فيه ثغرة حقيقية في `lib/session.ts`: مفتاح توقيع جلسة الدخول (session cookie) كان بيرجع لـ `TELEGRAM_WEBHOOK_SECRET` (مفتاح تاني خالص، بيتحط في رابط الـ webhook وأقل حماية)، ولو ده مش موجود كان بيستخدم نص ثابت مكتوب في الكود نفسه (`flowcash_dev_secret`). أي حد يعرف الاثنين دول (أو حتى يخمن النص الثابت لو نسيت تحط `TELEGRAM_WEBHOOK_SECRET` في Vercel) كان يقدر يزوّر كوكيز دخول لأي مستخدم عندك، من غير ما يعرف الـ PIN بتاعه خالص. اتصلح دلوقتي بمفتاح مخصص (`SESSION_SECRET`) — تفاصيل التفعيل تحت في "خطوة لازم تعملها".

## 1) اللي اتصلح في الكود مباشرة (مش محتاج منك حاجة غير الرفع)

- **مفتاح الجلسة (Session Secret)** — بقى مفتاح مستقل `SESSION_SECRET` بدل ما يشارك مفتاح الـ Telegram webhook، ومفيش نص ثابت احتياطي في وضع الإنتاج.
- **رؤوس الأمان (Security Headers)** — أضفتها في `next.config.ts`: Content-Security-Policy، X-Frame-Options (يمنع حد يحط موقعك جوه iframe لخداع المستخدم)، X-Content-Type-Options، Strict-Transport-Security، Referrer-Policy، Permissions-Policy.
- **نقلت السكريبت الوحيد اللي كان مكتوب inline** (بتاع الوضع الليلي وحجم الخط) لملف منفصل `public/theme-init.js` — عشان الـ CSP يقدر يمنع أي سكريبت غريب من غير ما يكسر التطبيق.
- **حماية تسجيل الدخول من التخمين (Rate Limiting)** — الـ PIN بتاعك رقمين بس (4 أرقام)، ومن غير حد لعدد المحاولات أي حد يقدر يجرب كل الاحتمالات (10000) في ثواني معدودة لأن Vercel بيسمح بمحاولات متوازية كتير. دلوقتي أي IP بيغلط 10 مرات في 15 دقيقة يتوقف مؤقتًا. ده محتاج جدول جديد في قاعدة البيانات (`login_attempts`) وطبّقته بالفعل.
- **فحص تسريب الأسرار تلقائيًا** — أضفت GitHub Action (`.github/workflows/security-scan.yml`) بيشغّل gitleaks على كل push/PR وأسبوعيًا على كل تاريخ المستودع، بالإضافة لفحص `npm audit` للمكتبات المعروف فيها ثغرات.
- **Dependabot** — أضفت `.github/dependabot.yml` عشان يبعتلك PR تلقائي لما أي مكتبة تحتاج تحديث أمني (لازم كمان تفعّله من إعدادات GitHub، تفاصيل تحت).
- **`.gitignore`** — كان أصلاً كويس (`.env*` و `*.pem` متجاهلين)، زودت عليه `*.key`, `credentials*.json`, `service-account*.json` وأنماط تانية شائعة.
- **فحصت تاريخ الـ commits المحلي** (3 commits بس موجودين هنا) — مفيش أي ملف `.env` أو مفتاح أو credential اتسجل فيهم. لو عندك تاريخ أطول على GitHub نفسه (من غير ما ترفعه من هنا)، الـ gitleaks Action اللي أضفته هيفحصه تلقائيًا أول ما يشتغل.

## 2) عزل مفاتيح الـ API — فحصت ومفيش حاجة محتاجة تتصلح

- مفيش أي مفتاح سري (Supabase service role، أو أي مفتاح تاني) بيبدأ بـ `NEXT_PUBLIC_` غير الـ URL بتاع Supabase نفسه (وده مش سري، آمن إنه يبان).
- `supabaseAdmin` (اللي بيحمل مفتاح Supabase الحساس) مستخدم بس في ملفات سيرفر (`lib/supabaseAdmin.ts`) ومحدش من الواجهة (client components) بيستورده.
- مفيش أي `fetch` من الواجهة بيروح لسيرفر خارجي مباشرة — كل حاجة بتعدي على `/api/*` بتاعتك، وده يبقى صح.

**تأكد بس إن `SUPABASE_KEY` في Vercel هو الـ Service Role Key مش الـ anon/publishable key** (تقدر تتأكد من صفحة API Settings في Supabase).

## 3) قاعدة البيانات — RLS (محتاج موافقتك، ملف SQL جاهز)

لقيت إن الـ Row Level Security متقفل على الـ 16 جدول كلهم. المهم تفهم السياق هنا: تطبيقك مش بيستخدم Supabase Auth أصلاً — بيستخدم نظام جلسة خاص بيك (كوكيز موقّعة)، وكل السيرفر بيتكلم مع قاعدة البيانات بمفتاح الـ Service Role اللي بيتخطى الـ RLS تلقائيًا في كل الأحوال. يعني تفعيل RLS مش هيغيّر حاجة في شغل التطبيق دلوقتي — لكنه هيقفل ثغرة احتياطية: لو مفتاح الـ anon اتسرّب يوم أو حد ضاف كود بيكلم Supabase من المتصفح بالغلط، من غيره كان هيقدر يشوف/يعدّل بيانات كل عملائك. مع RLS مفعّل وبدون أي policies، الافتراضي إنه يرفض كل حاجة لحد ما تحدد.

الملف `SECURITY-RLS.sql` جاهز — **متطبقوش أنا تلقائيًا** زي ما طلب مني النظام، افتحه في SQL Editor بتاع Supabase وشغّله لما تكون مطمئن.

## 4) الـ PWA والتخزين المحلي — فحصت ومفيش حاجة حساسة متخزنة

- الـ Service Worker (`public/sw.js`) بيخزن بس شكل الصفحات (الهيكل الفاضي)، مش بيانات الحسابات/الحركات — دي بتتجاب لايف من الـ API مش من الكاش.
- الـ localStorage بيخزن بس تفضيل الوضع الليلي وحجم الخط — مفيش بيانات مالية أو حساسة فيه.

## 5) التشويش/الضغط (Minification & Obfuscation)

Next.js بيضغط ويقصّر كود الواجهة تلقائيًا في أي build إنتاجي (مفعّل افتراضيًا، مفيش حاجة تتظبط). بس محتاج أكون صريح معاك: أي كود JavaScript بيشتغل في المتصفح لازم يكون قابل للقراءة/فك التشفير بشكل أو بآخر عشان يشتغل أصلاً — التشويش (Obfuscation) بيصعّب القراءة شوية بس مش حماية حقيقية ضد حد مصمم يفهم الكود، وممكن كمان يبطّئ التطبيق. الحماية الحقيقية إن أي منطق أو مفتاح حساس (زي ما إحنا عملناه) يفضل في السيرفر خالص وميوصلش للمتصفح أصلاً — وده متأكد منه بالفعل.

## 6) تقييد الطلبات (Rate Limiting) — اتعمل لأهم نقطة، والباقي اختياري

عملت الحماية لأخطر نقطة (تسجيل الدخول بالـ PIN). باقي الـ APIs مش عندها حد أقصى للطلبات دلوقتي — على خطتك الحالية (Vercel Hobby) الحماية الكاملة من هجمات DDoS بتحتاج خطة Pro (فيها WAF/Attack Challenge Mode). لو حابب أضيف حد أقصى على endpoints تانية معينة (مثلاً استيراد كشف حساب أو أي حاجة بتستهلك موارد) قولّي أضيفه.

---

## خطوة لازم تعملها بنفسك (مرة واحدة بس) — وإلا الموقع هيبوظ

ضيف متغير بيئة جديد في Vercel (Project Settings → Environment Variables):

```
SESSION_SECRET=fjeB7jXoab/wXLINQQ9q2zyLYItKmbbdAKl6GFCere1nhn9C+E0dojJ0Htim+7+g
```

(القيمة دي مُولّدة عشوائيًا هنا، آمن تستخدمها زي ما هي، أو تولّد وحدة بنفسك بنفس الطول). لو معملتش الخطوة دي قبل ما ترفع الكود، الموقع هيدي خطأ 500 في أي صفحة — ده متعمّد (أحسن من إنه يشتغل بمفتاح ضعيف تحت الطاولة) بس لازم تعمله الأول.

**ملحوظة:** بعد ما تحط المفتاح الجديد، كل المستخدمين (وأنت كمان) هيتطلب منهم يسجلوا دخول تاني — ده طبيعي ومتوقع لأن كل الجلسات القديمة اتوقّعت بمفتاح مختلف.

## قائمة تحقق يدوية في إعدادات GitHub (Settings)

- [ ] **Branch protection** على `main`: Settings → Branches → Add rule → امنع الـ push المباشر، اطلب Pull Request (لو هتشتغل مع حد تاني مستقبلاً — لو لسه لوحدك مش ضروري بإلحاح).
- [ ] **Secret scanning + Push Protection**: Settings → Code security → فعّل "Secret scanning" و"Push protection" (بيمنع أي commit فيه مفتاح واضح من الأساس قبل ما يوصل للمستودع).
- [ ] **Dependabot alerts + security updates**: نفس الصفحة (Code security) — فعّلهم عشان تتاخد بالك من أي مكتبة فيها ثغرة معروفة.
- [ ] **Private repo** — تأكد إنه لسه Private (مذكور إنه كذلك بالفعل).

## اللي محتاج فحص إضافي لو حابب (مقترح مستقبلي)

- الـ Telegram webhook بيتأكد من السر عن طريق رابط (`?secret=...`) — تليجرام بيدعم طريقة أأمن (`secret_token` بيتبعت كـ header بدل ما يبان في الرابط). مش عاجل لكنه تحسين لطيف لو حبينا نعمله في جولة جاية.
